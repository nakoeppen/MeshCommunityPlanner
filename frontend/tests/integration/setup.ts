/**
 * Playwright global setup — referenced by playwright.config.ts
 * Minimal no-op; Playwright connects to the live dev server.
 */

export default async function globalSetup() {
  // No-op — dev server started by Playwright webServer config
}
